extends layout
block content
	.row
		.col-lg-2
			.row
				.col-sm-12
					table#activity-users.table.table-striped.table-bordered.table-hover
						tbody
							tr
								th Users
							if users
								each user, i in users
									tr
										td
											a(href="#" onclick="loadActivity('#{user.username}')") #{user.name}
		.col-lg-10
			h1.page-header Activities
			.row.page-title-tools
				button.btn.btn-default(onclick='reloadEvents();') Reload
			#calendar
			